import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {

    },
    headingText: {
        color: 'black',
        fontSize: 20,
        textAlign:'center'
    }

})

export default styles